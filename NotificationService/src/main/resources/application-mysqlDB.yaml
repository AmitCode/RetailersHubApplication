spring:
  datasource:
    url:jdbc : mysql://localhost:3306/THE_NOTIFICATION_INFOS
    username : root
    password : "system123#"
  jpa:
    properties:
      hibernate:
        dialect:org.hibernate.dialect.MySQL8Dialect
      format : true
    hibernate:
      ddl-auto : true
    show-sql : true

server:
  port:8090
