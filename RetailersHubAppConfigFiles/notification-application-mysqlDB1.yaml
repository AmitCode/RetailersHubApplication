spring:
  datasource:
    #url: jdbc:mysql://${DB_HOST:host.docker.internal}:3306/${DB_NAME:NOTIFICATION_SERVICE_DATABASE}
    url: jdbc:mysql://localhost:3307/notification_services_database
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
